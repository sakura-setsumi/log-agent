# Local tooling and editor settings
.codex/
.idea/
# Agent working memory (session notes, screenshots) - not project source.
.workbuddy-ai/

# Build artifacts
log-agent.exe
log-agent-linux-amd64
dozzle-ops*.exe
dist/
.gocache/
*.test

# Local configuration and secrets
.env
.env.*
!.env.example
log-agent-settings.json
# The portable config folder, created beside the executable. It can hold
# plaintext API keys, so it must never be committed.
data/
